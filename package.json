{
  "scripts": {
    "clean": "rimraf dist .tmp-run",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "example": "npm run build && node dist/debug/react_agent_example.js"
  },
  "dependencies": {
    "@langchain/community": "^0.3.57",
    "@langchain/core": "^0.3.78",
    "@langchain/langgraph": "^0.4.9",
    "@langchain/openai": "^0.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
  "rimraf": "^5.0.5",
    "tsx": "^4.19.1",
    "typescript": "^5.9.3"
  }
}
